# Reachable from the iPad. localhost only works in a simulator on the same machine.
# For a physical iPad from WSL, use an ngrok / Cloudflare Tunnel URL, or a
# Windows host IP you can actually reach. Restart Metro after changing this.
EXPO_PUBLIC_API_BASE=http://localhost:8000
